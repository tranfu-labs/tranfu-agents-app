# ADR-0002 不追踪 token/成本;写凭证仅 TF_KEY

- 状态:Accepted
## 背景
本项目是"谁在干什么"的协作可观测,不是计费系统;混入 token/成本会带来隐私与误导。
## 决策
数据模型/协议/UI **不包含任何 token 或费用**字段(历史上的相关代码与"token"字样已清除)。
唯一的写入凭证是 `TF_KEY`,通过请求头 `X-TF-Key` 校验。
## 后果
- ✅ 协议简单、隐私友好。
- 约束:禁止以任何形式把成本/计费概念带回。
