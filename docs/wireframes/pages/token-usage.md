# Token Usage · 线框图

> 路由：`/token-usage`（`frontend/src/views/TokenUsage.tsx`）。画法与硬规则见 [../AGENTS.md](../AGENTS.md)，控件画法见 [../legend.md](../legend.md)。
> 状态规则：全部可见筛选与表格排序写入 URL query；详情抽屉、选中 KEY 与忽略风险为临时状态，不写 URL 或浏览器存储。

比例尺：1 显示列 = 12px（横，全角字符占 2 列）｜ 1 行 = 24px（纵）
断点：桌面 1440×900（120 列）｜ 平板 768×1024（64 列）｜ 手机 375×812（31 列）

## 字符图

```text
━━ 桌面 1440×900（120 列 × 38 行）━━
┌─ 顶部导航 ───────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│◆ TRANFU//AGENTS  Pods  Agents  SKILLS  ‹Token 用量›                +N 7天新发现  N Skill资产  ●N Live  [中|EN] [主题]│
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

┌─ ① 控制条 ───────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│时间范围[30天▾]  图表维度[天▾]  KEY类型[Dapp▾]  模型[gpt-5.5▾]  状态[全部▾]  Top[20▾]  搜索[________]                 │
│自选时间时展开：开始[YYYY-MM-DD HH:mm]  结束[YYYY-MM-DD HH:mm]                                                        │
│筛选/排序 replace 写入 URL；默认值省略；刷新、复制链接、历史导航从 URL 恢复                                           │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

┌─ ② 摘要与健康 ───────────────────────────────────────────────────────────────────────────────────────────────────────┐
│消耗金额  增长率  月度预测  Tokens  请求数  活跃KEY  失败率  风险KEY                                                  │
│性能健康：成功率  平均延迟  P95  吞吐量  模型成功率  KEY失败率                                                        │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

┌─ ③ 主分析 ───────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│KEY 消耗排行（Top N）                              │ 风险提示（忽略为临时态）                                         │
│每日使用趋势（当前 URL 时间范围与图表维度；本地筛选后的 KEY 集合）                                                    │
│类型消耗占比 / 模型消耗占比                     │ 模型分析                                                            │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

┌─ ④ KEY 明细 ─────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│[全部] [只看个人] [只看Dapp]  ☐隐藏0消耗  [导出CSV]                                                                   │
│KEY名称↕ │ 类型↕ │ 归属↕ │ 消耗金额↕ │ 请求数↕ │ 常用模型 │ 剩余额度↕ │ 趋势 │ 状态↕ │ 最后使用↕                      │
│快捷类型、隐藏0消耗、表头排序与顶部控件共用 URL query；点击行打开临时详情抽屉                                         │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

┌─ ⑤ KEY 详情抽屉 ─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│同页右侧浮层：用量 / 模型 / 风险 / 趋势 / 额度；关闭或刷新后恢复默认，不写 URL                                        │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

━━ 平板 768×1024（64 列 × 43 行）━━
┌─ 顶部导航 ───────────────────────────────────────────────────┐
│◆ TRANFU  Pods Agents SKILLS ‹Token用量›  [中|EN] [主题]      │
└──────────────────────────────────────────────────────────────┘

┌─ ① 控制条 ───────────────────────────────────────────────────┐
│时间[30天▾]  维度[天▾]  类型[Dapp▾]                           │
│模型[gpt-5.5▾]  状态[全部▾]  Top[20▾]                         │
│搜索[________________]                                        │
│custom: 开始[日期时间] 结束[日期时间]                         │
│全部筛选 replace 写 URL；刷新/复制可恢复                      │
└──────────────────────────────────────────────────────────────┘

┌─ ② 摘要与健康 ───────────────────────────────────────────────┐
│8 格摘要（4×2）                                               │
│成功率 / 延迟 / P95 / 吞吐量                                  │
└──────────────────────────────────────────────────────────────┘

┌─ ③ 主分析 ───────────────────────────────────────────────────┐
│KEY 消耗排行                                                  │
│风险提示                                                      │
│每日使用趋势                                                  │
│类型/模型占比                                                 │
│模型分析                                                      │
└──────────────────────────────────────────────────────────────┘

┌─ ④ KEY 明细 ─────────────────────────────────────────────────┐
│快捷类型  ☐隐藏0消耗  [导出CSV]                               │
│表格仅自身横向滚动；排序写 URL                                │
└──────────────────────────────────────────────────────────────┘

┌─ ⑤ 详情抽屉 ─────────────────────────────────────────────────┐
│同页浮层；临时态不写 URL                                      │
└──────────────────────────────────────────────────────────────┘

━━ 手机 375×812（31 列 × 34 行）━━
┌─ 顶部导航 ──────────────────┐
│◆ TRANFU  ‹Token›            │
│[中|EN] [主题]               │
└─────────────────────────────┘

┌─ ① 控制条 ──────────────────┐
│时间[30天] 维度[天]          │
│类型[Dapp] 模型[gpt5]        │
│状态[全部] Top[20]           │
│搜索[________]               │
│筛选写URL，可刷新恢复        │
└─────────────────────────────┘

┌─ ② 摘要 ────────────────────┐
│8格摘要（2×4）               │
│性能健康                     │
└─────────────────────────────┘

┌─ ③ 主分析 ──────────────────┐
│KEY排行                      │
│风险提示                     │
│使用趋势                     │
│占比 / 模型                  │
└─────────────────────────────┘

┌─ ④ KEY 明细 ────────────────┐
│快捷筛选 / 隐藏0             │
│表格内横滚；排序写URL        │
└─────────────────────────────┘

┌─ ⑤ 详情抽屉 ────────────────┐
│同页浮层，不写URL            │
└─────────────────────────────┘
```

| 编号 | 元素 | 状态/交互 | 数据来源 | 引用控件 |
|---|---|---|---|---|
| ① | 控制条 | `w/wstart/wend/g/kind/model/risk/topn/q` replace 写 URL；手机单列铺开；默认值省略 | 当前 URL + Token Usage payload | Skills query-state 控制条 |
| ② | 摘要与健康 | 使用当前筛选后的 KEY 集合计算事实 | 当前与上一时间范围 payload | 现有 Token Usage stats/health |
| ③ | 主分析 | Top N、搜索、类型、模型、风险联动排行/趋势/归因 | 当前 payload 前端派生 | Token Usage 图表 |
| ④ | KEY 明细 | 快捷类型与顶部共用 `kind`；`hz/sort/dir` 写 URL；平板/手机仅表格容器横滚 | 当前 payload 前端派生 | Skills 明细筛选/排序 |
| ⑤ | 详情抽屉 | 点击行打开，关闭或刷新恢复默认，不写 URL | 当前选中 KEY | 现有 Token Usage drawer |
