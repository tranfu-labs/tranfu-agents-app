# 任务：fix-dev-review-findings

- [x] 修复后端 placeholder 过滤大小写、overview/evidence 口径和 published previous count。
- [x] 补充后端 SKILLS 统计测试覆盖。
- [x] 修复前端 evidence 请求竞态、load-more 显示和 offset 续载。
- [x] 补充前端 evidence 单元测试覆盖。
- [x] 修复 workflow tag 默认/校验、compose volume、healthcheck port。
- [x] 跑 `python -m py_compile server/*.py server/routes/*.py`。
- [x] 跑 `python -m pytest -q`。
- [x] 跑 `npm --prefix frontend run test:unit`。
- [x] 跑 `npm --prefix frontend run build`。
