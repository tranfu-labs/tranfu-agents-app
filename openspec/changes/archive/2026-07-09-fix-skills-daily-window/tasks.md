# 任务:fix-skills-daily-window

- [x] 1. 在前端补 date-only 闭区间 helper 和图表轴选择逻辑,优先使用 `overview.window.start..end`。
- [x] 2. 将 `/skills` 主趋势卡标题与 `aria-label` 改为当前时间窗 i18n 派生文案,按人视角保留后缀。
- [x] 3. 增加单元测试覆盖窗口轴生成和中英文标题。
- [x] 4. 运行 `npm --prefix frontend run test:unit`。
- [x] 5. 运行 `npm --prefix frontend run build`。
- [x] 6. 对照方案确认没有改动后端口径、路由状态同步或页面布局。
