{
  "_comment": "合并进 ~/.claude/settings.json,把实时状态上报到看板。",
  "env": {
    "TF_SERVER": "https://agents.tranfu.io",
    "TF_KEY": "REPLACE_ME",
    "TF_OPERATOR": "alice",
    "TF_RUNTIME": "claude-code",
    "TF_AGENT": "code"
  },
  "hooks": {
    "UserPromptSubmit": [
      { "hooks": [ { "type": "command",
        "command": "source ~/.tranfu/tf_client.sh && TF_SESSION=$CLAUDE_SESSION_ID tf_emit running --task \"$(echo \"$CLAUDE_USER_PROMPT\" | head -c 120)\" --step started" } ] }
    ],
    "Stop": [
      { "hooks": [ { "type": "command",
        "command": "source ~/.tranfu/tf_client.sh && TF_SESSION=$CLAUDE_SESSION_ID tf_emit done --step finished" } ] }
    ]
  }
}
